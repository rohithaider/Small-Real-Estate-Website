export default function Button({value,name}){
    return(
        <a href="https://links.tailgrids.com/play-download"
                  className={value}>
                  {name}
                </a>
    )
}